# Xcode / сборка
build/
DerivedData/
*.xcuserstate
**/xcuserdata/
.DS_Store

# project.yml — источник; сам .xcodeproj коммитим, чтобы друг открывал одним кликом

# Ключи. Репозиторий публичный, поэтому настоящий ключ MapKit не коммитим.
# Secrets.xcconfig (базовый, пустой) — В git; реальный ключ кладётся рядом,
# в Secrets.local.xcconfig, который git игнорирует. Без него сборка идёт,
# просто вместо карты подсказка.
Localee/Secrets.local.xcconfig
