# Xcode / сборка
build/
DerivedData/
*.xcuserstate
**/xcuserdata/
.DS_Store

# project.yml — источник; сам .xcodeproj коммитим, чтобы друг открывал одним кликом

# Ключи. Репозиторий публичный, поэтому настоящий Secrets.plist не коммитим —
# в репозитории лежит только шаблон Secrets.example.plist в корне.
# Без этого файла приложение собирается и работает, просто вместо карты подсказка.
Localee/Secrets.plist
